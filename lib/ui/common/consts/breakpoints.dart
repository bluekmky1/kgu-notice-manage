class Breakpoints {
  // Screen width breakpoints
  static const double mobile = 600.0;
  static const double tablet = 900.0;
  static const double desktop = 1200.0;

  // Content width for each breakpoint
  static const double mobileContentWidth = double.infinity;
  static const double tabletContentWidth = 500.0;
  static const double desktopContentWidth = 800.0;

  // Padding for each breakpoint
  static const double mobilePadding = 16.0;
  static const double tabletPadding = 32.0;
  static const double desktopPadding = 48.0;
}
