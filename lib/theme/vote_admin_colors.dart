import 'package:flutter/material.dart';

class VoteAdminColors {
  // Primary Colors
  static const Color primary = Color(0xFF002968); // 명지대 네이비
  static const Color onPrimary = Color(0xFFFFFFFF); // 흰색

  // Secondary Colors - 밝은 블루 계열
  static const Color secondary = Color(0xFF4A90E2); // 밝은 파랑
  static const Color onSecondary = Color(0xFFFFFFFF); // 흰색

  // Background Colors
  static const Color background = Color(0xFFFFFFFF); // 흰색
  static const Color onBackground = Color(0xFF1A1A1A); // 거의 검정

  // Surface Colors
  static const Color surface = Color(0xFFF5F7FA); // 매우 연한 회색
  static const Color onSurface = Color(0xFF1A1A1A); // 거의 검정

  // Error Colors
  static const Color error = Color(0xFFDC3545); // 빨간색
  static const Color onError = Color(0xFFDC3545); // 빨간색

  // Additional Colors
  static const Color accent = Color(0xFF00A3FF); // 강조색 (밝은 파랑)
  static const Color disabled = Color(0xFFE0E0E0); // 비활성화 색상
  static const Color divider = Color(0xFFEEEEEE); // 구분선 색상

  // Text Colors
  static const Color textPrimary = Color(0xFF1A1A1A); // 기본 텍스트
  static const Color textSecondary = Color(0xFF757575); // 부가 텍스트
  static const Color textHint = Color(0xFFBDBDBD); // 힌트 텍스트

  // Common Colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color transparent = Color(0x00000000);
}
